{
  "name": "pfs-etc-web-tooling",
  "private": true,
  "description": "Markdown formatting/lint tooling for Claude Code hooks (see .claude/hooks/format_markdown.sh)",
  "devDependencies": {
    "markdownlint-cli2": "^0.23.2"
  }
}
